# This file is generated by kubebuilder
apiVersion: certmanager.k8s.io/v1alpha1
kind: Issuer
metadata:
  name: selfsigned-issuer
  namespace: system
spec:
  selfSigned: {}
---
apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: serving-cert # same name as the one appeared in kustomizeconfig.yaml
  namespace: system
spec:
  commonName: $(SERVICENAME).$(NAMESPACE).svc # $(SERVICENAME) and $(NAMESPACE) will be substituted
  dnsNames:
  - $(SERVICENAME).$(NAMESPACE).svc.cluster.local # $(SERVICENAME) and $(NAMESPACE) will be substituted
  issuerRef:
    kind: Issuer
    name: selfsigned-issuer
  secretName: webhook-server-cert # this secret will not be prefixed, since it's not managed by kustomize
